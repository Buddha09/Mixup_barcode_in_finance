# Pinned to the versions the pipeline was verified against.
# yfinance is needed ONLY for "--stage fetch". Once data/raw/ is populated the
# entire pipeline runs offline and you can uninstall it if you like.
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
joblib>=1.3
matplotlib>=3.7
tqdm>=4.65
ripser>=0.6.4
persim>=0.3.2
yfinance>=0.2.40      # only needed for the first download; cached CSVs thereafter
# optional, auto-detected by the ML stage:
# xgboost>=2.0
# lightgbm>=4.0
